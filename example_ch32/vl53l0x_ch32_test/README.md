# vl53l0x_ch32_test

ESP32 通过共享 CAN 核心动态发现 CH32-I2C 网关，等待 F2 确认，按
`device_type + token` 维护稳定节点，并在下游地址 `0x29` 初始化 VL53L0X。

串口每 200 ms 输出一次距离，共输出 50 次；距离超过 2000 mm 时输出
`OUT RANGE`。例程每 5 秒执行增量重发现，节点 ID 变化时原驱动句柄继续
引用同一个稳定节点记录。
