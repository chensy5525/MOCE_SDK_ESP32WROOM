你正在基于一个已经存在并已验证可用的嵌入式 SDK 开发应用程序。

请严格遵守以下约束：

【项目背景】
- 该 SDK 已经完成基础搭建，官方底座、BSP、组件、工具链均已存在。
- examples_ch32/ 目录中已有可工作的参考示例，可作为接口使用方式的参考。
- 当前任务是基于已有 SDK 和 BSP 开发“用户应用”，而不是修改 SDK 本身。

【目录改动边界】
你只允许创建、修改或覆盖 project/ 目录下的内容。
你绝对不允许修改以下目录中的任何文件：
- components/
- boards/
- tools/
- env/
- third_party/
- examples_ch32/

如果你认为功能不足，也不能修改 SDK，只能在 project/ 下通过已有接口完成功能。

【接口使用原则】
- 优先复用现有 BSP 和 SDK 接口。
- 不要重新实现 SDK 已有能力。
- 不要绕过 BSP 直接随意访问底层硬件，除非项目已有示例明确允许。
- 若 examples_ch32/ 中已有类似实现方式，优先沿用其调用风格和工程结构。

【输出要求】
- 必须生成完整可编译的应用工程。
- 必须同时生成项目所需的 CMakeLists.txt。
- 输出时按文件路径分别给出文件内容。
- 只输出 project/ 下的文件，不要输出其他目录文件。
- 若需要多个源文件，可在 project/<project_name>/main/ 下自行组织。
- 代码应可直接用于当前 SDK 的构建流程。

【代码风格要求】
- 代码尽量模块化、清晰、可维护。
- 增加必要注释，但不要过度冗长。
- 避免引入无必要的复杂依赖。
- 若使用 FreeRTOS 任务/延时/状态机，应保持结构简单明确。

【禁止事项】
- 禁止修改 SDK 原有组件代码。
- 禁止建议用户去修改 SDK 内部实现来完成当前任务。
- 禁止输出与 project/ 无关的补丁、diff 或重构建议。
- 禁止省略 CMakeLists.txt。

在开始生成代码前，请先基于上述约束进行设计，再输出最终工程文件。

输出时请严格遵守以下格式：

===== FILE: project/<project_name>/CMakeLists.txt =====
<完整文件内容>

===== FILE: project/<project_name>/main/CMakeLists.txt =====
<完整文件内容>

===== FILE: project/<project_name>/main/main.c =====
<完整文件内容>

如有额外源文件或头文件，继续按相同格式输出。
不要输出任何非文件内容说明。
不要输出 SDK 目录下的文件。